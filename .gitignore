node_modules/
.DS_Store
*.log

# Локальный конфиг и секреты — НИКОГДА не коммитить
config.local.js
.env
.env.*
backend/.env

# Визуальный компаньон брейншторма (временные макеты)
.superpowers/

# Локальные настройки/состояние Claude Code
.claude/

# Рабочие временные файлы скриптов (дампы, промежуточные json)
.tmp/

# Next.js
/.next
/out
next-env.d.ts
*.tsbuildinfo
.env.local
.env.production

# Исходники картинок-обложек (в прод идёт копия в public/covers/)
IMAGE_TOWNS/

# Скриншоты гида Michelin — ИСТОЧНИК данных доски «Мишлен» (перенесены в
# lib/michelinDemo.ts). В рантайме не используются (карточки текстовые), в прод
# не нужны — иначе 55 МБ улетят в out/. Держим только локально.
public/Мишлен/
