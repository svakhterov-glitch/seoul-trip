{
  "name": "seoul-trip",
  "version": "1.0.0",
  "private": true,
  "description": "Планировщик путешествий: карта, расписание по дням, настройки поездки",
  "scripts": {
    "dev": "npx --yes serve .",
    "start": "npx --yes serve .",
    "check": "for f in $(find js -name '*.js'); do node --check \"$f\" || exit 1; done && echo 'syntax OK'"
  }
}
