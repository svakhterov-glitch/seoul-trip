/* ============================================================
   ШАБЛОН ЛОКАЛЬНОГО КОНФИГА
   Скопируйте этот файл в config.local.js (он в .gitignore) и
   впишите свои значения. Без config.local.js приложение работает
   локально: данные в localStorage, ИИ — заглушка.

   ВАЖНО: config.local.js НЕ коммитить. Это публичные клиентские
   ключи (anon key Supabase предназначен для браузера и защищён
   RLS) — но всё равно держим их вне репозитория.
   ============================================================ */

export default {
  // Supabase: хранение и синхронизация поездок между устройствами.
  // Требует включённой авторизации (Supabase Auth) — см. backend/README.md.
  // Убери блок supabase, чтобы остаться на localStorage.
  supabase: {
    url: "https://ВАШ_ПРОЕКТ.supabase.co",
    anonKey: "ВАШ_ANON_KEY",
    // accessToken: "...", // JWT пользователя после логина (когда добавим вход)
  },

  // База edge-функций (resolve-link, generate-itinerary, trends).
  // Обычно: https://ВАШ_ПРОЕКТ.functions.supabase.co
  // Убери, чтобы разбор ссылок и ИИ работали во фронтовом (упрощённом) режиме.
  functionsUrl: "https://ВАШ_ПРОЕКТ.functions.supabase.co",
};
