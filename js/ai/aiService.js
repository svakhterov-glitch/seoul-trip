/* ============================================================
   ИНТЕРФЕЙС ИИ-СЕРВИСА
   Контракт генерации маршрута. Сегодня его реализует
   MockAiService (отдаёт seed-места Сеула), завтра —
   ApiAiService (серверная функция + Claude API + растущая
   база «опыта путешественников»). Тот же контракт — замена
   реализации не трогает UI.
   ============================================================ */

export class AiService {
  /**
   * Сгенерировать места и разложить их по дням поездки.
   * @param {Trip} trip — поездка (город, даты, дни)
   * @param {object} prefs — предпочтения (интересы, темп, бюджет)
   * @returns {Promise<Place[]>} список мест с проставленными dayNumber/time
   */
  async generateItinerary(trip, prefs = {}) {
    throw new Error("not implemented");
  }
}
